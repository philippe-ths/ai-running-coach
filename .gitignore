# OS
.DS_Store

# Environment files (keep local)
.env
.env.*
!.env.example
!.env.sample
!.env.template
backend/.env
frontend/.env.local
!backend/.env.example
!frontend/.env.example

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
*.egg
.eggs/

# Python virtualenvs
.venv/
venv/
backend/.venv/

# Node / Next
node_modules/
frontend/node_modules/
.next/
frontend/.next/
out/
frontend/out/

# TypeScript build info
*.tsbuildinfo
frontend/tsconfig.tsbuildinfo

# Logs
*.log

# IDE
.idea/

# AI workflow and skills (tooling config, not product code)
# These overlap the installer's managed block below and are kept deliberately.
# They are broader than what the installer manages (.agents/ rather than only
# .agents/skills/, and .github/* with a workflows exception), and since
# workflow 3.25.1 the installer builds its block as a union and leaves a
# matching line outside it alone, so these survive an update untouched.
.agents/
.gemini/
.github/*
!.github/workflows/
claude.md
CLAUDE.md
.claude/
.codex/
.vscode/
AGENTS.md
GEMINI.md
.vercel
.env*

# make coach-review generated review artifacts (regenerated locally; see the
# Makefile coach-review target). Durable audit reports keep distinct names
# (performance-audit-*, multi-user-readiness-audit-*, knowledge-base-alignment-*)
# and stay tracked.
docs/audit/coach-*

# >>> ai-workflow (vendored, managed by installer) >>>
ai-workflow.md
.ai-policy/
.githooks/
CLAUDE.md
.claude/
AGENTS.md
.codex/
.agents/skills/
GEMINI.md
.gemini/
.github/copilot-instructions.md
.github/hooks/
.vscode/
# <<< ai-workflow <<<
